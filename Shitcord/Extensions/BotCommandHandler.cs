using Shitcord;
using DSharpPlus;
using Shitcord.Services;
using DSharpPlus.EventArgs;

public class BotCommandHandler
{
    BotConfig? config { get; }
    CustomCommandService? CCommands { get; }

    // Should execute both dynamic and static commands (and also other stuff)
    public BotCommandHandler() {

    }

    
}
